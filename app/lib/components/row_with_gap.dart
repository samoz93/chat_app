import 'package:flutter/material.dart';

class RowWithGaps extends StatelessWidget {
  const RowWithGaps({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
